void findMissingRepeatingNum(vector<int> &nums) {
  if(nums.size()==0)  retur;
  for(int i=0;i<nums.size();i++) {
    if(nums[abs(nums[i])-1]>0)
      nums[abs(nums[i])-1] = -nums[abs(nums[i])-1];
    else
      dup = abs(nums[i]);
  }
  for(int i=0;i<nums.size();i++) {
    if(nums[i]>0)
      missing = i+1;
  }

}
